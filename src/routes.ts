/*

This file outlines the API routes using the HATEOS URL Manager
library.  This library helps with the construction of links
in JSON API Responses.  

*/

import { Route } from '@tkottke/hateos-url-manager';

const v1API = new Route('/api/v1');